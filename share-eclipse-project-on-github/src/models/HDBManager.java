package models;

public class HDBManager extends User{
    public HDBManager(String NRIC, String password, boolean maritalStatus, int age) {
        super(NRIC, password, maritalStatus, age);
    }
}
